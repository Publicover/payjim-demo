<%= form_for @company do |f| %>

  <% if @company.errors.any? %>
    <div id="error_explanation">
      <p>
        <%= pluralize(@company.errors.count, "error") %> prohibited
        this article from being saved:
      </p>
      <% @user.errors.full_messages.each do |msg| %>
        <li><center><%= msg %></li>
      <% end %>
    </div>
  <% end %>

  <p>
    <%= f.label 'Name' %>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label 'Address Line 1' %>
    <%= f.text_field :add_ln1 %>
  </p>

  <p>
    <%= f.label 'Address Line 2' %>
    <%= f.text_field :add_ln2 %>
  </p>

  <p>
    <%= f.label 'Suite' %>
    <%= f.text_field :suite %>
  </p>

  <p>
    <%= f.label 'City, State and ZIP' %>
    <%= f.text_field :city_state_zip %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>
